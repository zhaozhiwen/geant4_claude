pdg>=0.2.2
# matplotlib + numpy power scripts/preview_gdml.py (sketch backend for
# the geant4-preview skill) and the canned analyze plots.
matplotlib>=3.6
numpy>=1.23
# uproot is the host-side ROOT reader for the geant4-analyze skill and
# scripts/validators/. Seeding it here means scripts/ensure_venv.sh installs
# (called by the skills that need Python) it into the managed venv, so the
# analyze/validate paths work on a fresh clone without a separate pip step
# (a uv-created venv ships no pip).
uproot>=5.0
