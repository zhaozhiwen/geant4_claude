# geant4_claude task — generated artifacts
# (the shared engine — .g4c/ cache/ venv/ — is gitignored at the PROJECT root,
# one dir up, not here.)
runs/*/
!runs/.gitkeep
*.root
build/
__pycache__/
.venv/
.ipynb_checkpoints/
# derived portable HTML produced by embed_html.py (regenerable)
*_portable.html
