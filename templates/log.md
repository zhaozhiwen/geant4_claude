# Project log

The record of every simulation task in this Geant4 project. The **geant4-task**
skill adds a Tasks-table row and a log entry per task; update the status and
result as a task progresses. Each task's *detailed* run log lives in its own
`<task>/log.md`.

## Tasks

| Task (dir) | Goal | Status | Result |
|------------|------|--------|--------|
| _none yet — run **geant4-task** to add one_ | | | |

## Log

<!-- Prepend a dated entry per task milestone, newest first. Template:

## YYYY-MM-DD — <task-dir>
- **Created / built / ran / analyzed** (which milestone).
- **Goal:** one line.
- **Outcome:** run id(s), key numbers, link to `<task>/report.html`.

-->
